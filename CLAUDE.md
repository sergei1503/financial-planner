# CLAUDE.md — Financial Planner (fplan_v2)

@AGENTS.md

Shared agent guidance lives in `AGENTS.md` (imported above) — stack, commands, boundaries,
testing rules. The project profile (stack profile, git user, PM, and the canonical dev-server
ports) lives in `.claude/CLAUDE.md`; don't duplicate it here.

Planning docs are in `docs/`: start any task from `docs/progress.md`, then `docs/plan.md`.
Design rationale is in `docs/decisions/` (ADRs).
